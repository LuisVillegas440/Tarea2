/*1; BOSQUEJO
La variable "a" toma el valor de 8 y la variable "b" toma el valor de 5.
Para a=8 y b=5, encuentra el valor de v = 2 * b + a div 2 + 4 * b mod a. */
const a = 8;
const b = 5;
const v = 2 * b + a / 2 + 4 * b % a;
console.log(v);
